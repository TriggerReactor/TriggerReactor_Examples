//'player' target
//'invname' name of storage

items = player.getInventory().getContents()
FOR i = 0:items.length
    IF items[i]
        {"inventorysave."+player+"."+invname+"."+i} = items[i]
    ENDIF
ENDFOR